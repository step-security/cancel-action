{
  "name": "cancel-action",
  "version": "0.4.0",
  "description": "GitHub Action to cancel the currently running workflow",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build cancel.js -o dist",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/step-security/cancel-action.git"
  },
  "keywords": [
    "github",
    "actions",
    "cancel",
    "workflow"
  ],
  "author": "step-security",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "axios": "^1.13.6"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
